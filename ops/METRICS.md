# Metrics ledger

Updated every Monday by the `metrics` routine. Sources: site `/api/stats`, GitHub API (stars, unique clones), Polar orders API, Fly billing.

| Week | Unique visitors | Pageviews | Stars | Unique clones | Pro sales | Team sales | Net revenue | Costs | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 2026-W36 | n/a (stats API blocked) | n/a (stats API blocked) | 0 | n/a (no traffic-API access) | 0 | 0 | $0 | ~$0 | Launch week. Org public, site deployed, Polar checkout wired, ops routines scaffolded. `anchorwatch.sh` unreachable from this environment's network policy (403 at egress proxy); GitHub traffic/clones has no tool path from this environment either. |

## Assessment log
- **2026-W36:** 2026-W36 was launch week: the org went live, anchorwatch.sh deployed, Pro/Team checkout wired to Polar, and the ops routines (release-watch, growth, support-triage, metrics) were scaffolded as GitHub Actions today. Zero stars, zero sales, zero open issues — expected for day one, before any distribution push (launch posts, Show HN, awesome-list PRs) has gone out. This run could not read `/api/stats`: the environment's network egress policy blocks `anchorwatch.sh` (403 at the agent proxy), so visitors/pageviews/top pages/referrers are "n/a" this week; unique clones is also "n/a" because no tool available in this environment reaches the GitHub traffic API. Two of the four routines — release-watch and growth — have never run: their GitHub Actions workflows were only wired up today (commit `3b911f7`) and are gated on a repo secret, `ANTHROPIC_API_KEY`, that is not set, so `scripts/run-routine.sh` will exit 0 ("routine skipped") the next time their cron fires rather than actually doing the work. Hypothesis: there's nothing to read yet — the real test starts once the owner adds the missing secret and posts the drafted launch content. Next experiment: add `ANTHROPIC_API_KEY` as a repo secret to unblock release-watch/growth/support-triage, post the launch drafts in `ops/drafts/launch-posts.md`, and get `anchorwatch.sh` added to the network allowlist so next Monday's stats aren't blind.
- **Gate status:** Phase 1 — 0/10 Pro sales, 0/300 stars cumulative, visitors unknown (blocked); day 1 of the 60-day window. Day-30 success line (≥5 Pro sales, ≥100 stars, ≥1,000 visitors/month) and day-60 kill line (<2 sales, <500 visitors/month) are both not yet evaluable — too early to call either way. Next meaningful check-in: ~2026-10-06 (day 30).
