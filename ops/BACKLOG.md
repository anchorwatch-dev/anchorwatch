# Backlog

- **hesreallyhim/awesome-claude-code submission** — only via the repo's web issue form, and only once the repo is ≥14 days old with continued commits, or ≥100 stars. Earliest: 2026-09-20. Human submits; agent drafts the text (see ops/drafts/awesome-list-entries.md).
- **Competitor watch**: agento-patronum (hooks that protect sensitive files/credentials/commands; listed on awesome-claude-code-toolkit). Compare rule coverage monthly; differentiate on tests, compound-command splitting, secret scanning, deny reasons.
- **Windows verification** of hooks under Git Bash (no CI runner exercises it yet).
- **Plugin eval suite** — Claude Code 2.1.269 adds `claude plugin eval` (cases under `evals/`, graders, `--ablation with-without` for a no-plugin baseline, JSON + HTML report, `--trust-plugin` for CI). Our tests prove the hook *scripts* decide correctly; an eval suite would prove the *model* does the right thing with a deny reason (explains it, proposes the safer path, does not retry or bypass) — the part the deny text is written for and nothing currently checks. Start with three cases: a blocked `rm -rf /`, a blocked `.env` write, and a warn-level `npm publish`.
- **Phase 2 prep**: sample "agent-readiness audit" report as a public page (see ops/ROADMAP.md).
- **Community marketplace**: submitted 2026-09-07 via platform.claude.com (status: pending review). When approved, verify `claude plugin install anchorwatch@claude-community` and update install docs/README to mention it.
- ~~Function hooks port~~ DONE 2026-09-10 as `plugins/anchorwatch-mod` (prototype, 181 tests, not marketplace-listed). Remaining: verify in a live 2.1.267+ session with the flag, port warn rules once an additive-context channel exists, port `.anchorwatch.json` overrides. Original note: Claude Code 2.1.263 ships a function-hooks prototype behind `CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1` (typed `tool.call` handlers in `hooks-handlers/*.ts`, `classic.*` wraps shell hooks 1:1, `/plugin-types` emits fixture types; see anthropics/claude-code#91870). Plan: keep the bash plugin as the shipping version, add an experimental `hooks-handlers/anchorwatch.ts` implementing the deny rules as a typed `tool.call` gate with a tsc-checked regression suite, and be the first guardrail plugin listed with function-hook support when the flag goes GA. Release-watch should track the flag, the `claude-code` npm types package, and docs pages mentioning function hooks.
