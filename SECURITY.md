# Security

Anchorwatch is a guardrail, not a security boundary; bypasses of pattern matching are expected and documented. Still, if you find a way the plugin itself could be abused (e.g. a hook that can be made to execute attacker-controlled content, or a config injection), please open a private security advisory on GitHub rather than a public issue. We aim to respond within 3 days.
